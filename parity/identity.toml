# Identity dictionary. One row per IdentityDomain plus target_weights.
# Docs rows are checked, never generated. `covers` restates the layer table
# of adr/0022-causal-compiler-contract.md in one line; keep them in step.

[[identity]]
domain = "target"
rust = "ContractIdentities.target"
contract_key = "identities.target"
python = "inspect().target_id"
availability = "prepared|loaded"
naming_row = "Target identity"
covers = "query, population, interventions, outcome functional, temporal policy, schema-name bindings"

[[identity]]
domain = "identification"
rust = "ContractIdentities.identification"
contract_key = "identities.identification"
python = "inspect().identification_id"
availability = "prepared|loaded"
naming_row = "Identification premises identity"
covers = "population-free question, accepted structure (posterior atoms and weights, class prior, transport selection and full optional evidence catalog including regimes, measurements, assignments, projections, snapshot bindings and target sampling), observation contract, RD premises"

[[identity]]
domain = "identification_product"
rust = "ContractIdentities.identification_product"
contract_key = "identities.identification_product"
python = "inspect().identification_product_id"
availability = "prepared|loaded"
naming_row = "Identification product identity"
covers = "status, estimands, expression arena, derivation rule ids, required assumptions, hedge, capped search"

[[identity]]
domain = "program"
rust = "ContractIdentities.program"
contract_key = "identities.program"
python = "inspect().program_id"
availability = "prepared|loaded"
naming_row = "Compiled program identity"
covers = "target, identification, products, licensed inferential commitments, completion budget"

[[identity]]
domain = "inference_binding"
rust = "ContractIdentities.inference_binding"
contract_key = "identities.inference_binding"
python = "inspect().inference_binding_id"
availability = "prepared|loaded"
naming_row = "Inference binding identity"
covers = "prior contents and mapping, backend and likelihood, draws and replicates, response and observation options, split, validation, overlap policy"

[[identity]]
domain = "observation"
rust = "ContractIdentities.observation"
contract_key = "identities.observation"
python = "inspect().observation_id"
availability = "prepared|loaded"
naming_row = "Observation identity"
covers = "schema and observation assumptions with their variables"

[[identity]]
domain = "data_snapshot"
rust = "ContractIdentities.data_snapshot"
contract_key = "identities.data_snapshot"
python = "inspect().data_snapshot_id"
availability = "prepared|loaded"
naming_row = "Data snapshot identity"
covers = "observation, modality, ordered partition content digests, regularity, interference network and assignment"

[[identity]]
domain = "execution"
rust = "ExecutionIdentityWire"
contract_key = "identities.execution"
python = "inspect().execution_id"
availability = "loaded"
naming_row = "Execution identity"
covers = "library version, seed, threads, backend and kernel policy, determinism, adaptive budgets"

[[identity]]
domain = "claim"
rust = "ClaimIdentityWire"
contract_key = "claim.claim_id"
python = "inspect().claim_id"
availability = "loaded"
naming_row = "Execution claim identity"
covers = "contract seal (identities, four slots, audit fields), claim fields, executed result body digest"

[[identity]]
domain = "score_reuse"
rust = "ScoreReuseIdentityWire"
contract_key = "identities.score_reuse"
python = "inspect().score_reuse_id"
availability = "prepared|loaded"
naming_row = "Score reuse identity"
covers = "identification, data snapshot, row index, folds, adjustment set, nuisance provenance, treatment, replicates, inference binding"

[[identity]]
domain = "target_weights"
rust = "TargetWeightsIdentityWire"
contract_key = "identities.target_weights"
python = "inspect().target_weights_id"
availability = "loaded"
naming_row = "Target-weight identity"
covers = "weight digest, row count, data snapshot, score reuse, declared parents"

[[identity]]
domain = "learned_trial"
rust = "LearnedTrialWire"
contract_key = "manifest.artifact_id"
python = "n/a (artifact_id in the learned-trial artifact manifest)"
availability = "loaded"
naming_row = "Learned-trial artifact identity"
covers = "graph, query, raw inputs, frozen options, seed, retained score and bootstrap evidence"

[[identity]]
domain = "transport_certificate"
rust = "TransportCertificateWire"
contract_key = "manifest.artifact_id"
python = "n/a (artifact_id in the transport-certificate artifact manifest)"
availability = "loaded"
naming_row = "Transport-certificate artifact identity"
covers = "structural transport certificate outcome (positive proof or refusal)"