# Pointer into conformance/ for the Tigramite PCMCI fixture.

id = "tigramite.pcmci_lag1"
capability_ids = [
 "discovery.ci.partial_corr",
 "discovery.pcmci",
]
fixture_dir = "conformance/discovery/pcmci_lag1"
tolerance_class = "Exact"
generator = "/tmp/oracle_harness_deleted_after_artifact_import.py"
baseline_pin = "parity/baselines/tigramite.toml"
