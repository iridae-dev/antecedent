# Reason table for refused cells. Refusal is the default: any cell that is
# not licensed and not n/a is `SupportRefusal::Refused` whether or not a
# rule here matches. This file does not cause refusal; it names it.
# A cell matching every listed constraint reports that rule's `reason`.
# Must not overlap licensed cells or n/a predicates (the gate fails on overlap).

[[closed]]
queries = ["Counterfactual"]
graph_classes = ["Admg", "Cpdag", "Pag"]
reason = "Counterfactual requires a supplied static Dag."

[[closed]]
queries = [
  "AverageDerivative",
  "DirectionalDerivative",
  "Elasticity",
  "PointDerivative",
  "ResponseJacobian",
  "SemiElasticity",
]
graph_classes = ["Admg", "Cpdag", "Pag"]
reason = "Derivatives require a supplied static Dag."


[[closed]]
queries = ["PathSpecificEffect", "InterventionalDistribution"]
graph_classes = ["Dag", "Admg", "Cpdag", "Pag"]
structures = ["graph_posterior"]
reason = "graph-posterior structures are refused: a path, distribution, or mediation mixture is not a single estimand across posterior atoms."

[[closed]]
queries = ["PathSpecificEffect"]
graph_classes = ["Admg", "Cpdag", "Pag"]
structures = ["accepted"]
reason = "Accepted path queries require a Dag."

[[closed]]
queries = ["InterventionalDistribution"]
graph_classes = ["Cpdag", "Pag"]
structures = ["accepted"]
reason = "Accepted InterventionalDistribution is licensed on Dag and Admg; Cpdag/Pag remain refused."

[[closed]]
queries = ["MediationEffect"]
graph_classes = ["Admg", "Cpdag", "Pag"]
reason = "MediationEffect requires a supplied static Dag."

[[closed]]
queries = ["AnomalyAttribution", "ChangeAttribution"]
graph_classes = ["Admg", "Cpdag", "Pag"]
reason = "AnomalyAttribution and ChangeAttribution require a supplied static Dag."

[[closed]]
queries = ["AnomalyAttribution", "ChangeAttribution"]
graph_classes = ["Dag"]
structures = ["accepted", "graph_posterior"]
reason = "Attribution is licensed only for an explicit Dag at validation none."

[[closed]]
queries = ["AnomalyAttribution", "ChangeAttribution"]
graph_classes = ["Dag"]
inferences = ["Bayesian"]
reason = "Attribution has no Bayesian draw path; the licensed cells are Frequentist."

[[closed]]
queries = ["AnomalyAttribution", "ChangeAttribution"]
graph_classes = ["Dag"]
validations = ["cheap", "full"]
reason = "Attribution has no native refuter suite; ATE refuters do not apply."

[[closed]]
queries = ["TransportQuery"]
graph_classes = ["Dag", "Cpdag", "Pag"]
reason = "Licensed TransportQuery is Admg-only; selection diagrams are ADMGs. Recursive sID outside the implemented subset is NotCertified."

[[closed]]
queries = ["TransportQuery"]
graph_classes = ["Admg"]
structures = ["accepted"]
reason = "TransportQuery is licensed only for an explicit Admg at validation none."

[[closed]]
queries = ["TransportQuery"]
graph_classes = ["Admg"]
structures = ["graph_posterior"]
inferences = ["Bayesian"]
reason = "TransportQuery graph-posterior mixtures are not staged."

[[closed]]
queries = ["TransportQuery"]
graph_classes = ["Admg"]
inferences = ["Bayesian"]
reason = "TransportQuery has no Bayesian trial-to-target estimator."

[[closed]]
queries = ["TransportQuery"]
graph_classes = ["Admg"]
validations = ["cheap", "full"]
reason = "TransportQuery has no native refuter suite; ATE refuters do not apply."

[[closed]]
queries = ["InterferenceQuery"]
graph_classes = ["Admg", "Cpdag", "Pag"]
reason = "Licensed InterferenceQuery is Dag-only; the graph binds schema/outcome, it does not identify the contrast."

[[closed]]
queries = ["InterferenceQuery"]
graph_classes = ["Dag"]
structures = ["accepted"]
reason = "InterferenceQuery is licensed only for an explicit Dag at validation none."

[[closed]]
queries = ["InterferenceQuery"]
graph_classes = ["Dag"]
structures = ["graph_posterior"]
inferences = ["Bayesian"]
reason = "InterferenceQuery graph-posterior mixtures are not staged."

[[closed]]
queries = ["InterferenceQuery"]
graph_classes = ["Dag"]
inferences = ["Bayesian"]
reason = "InterferenceQuery is design-based Frequentist; there is no Bayesian randomization estimator."

[[closed]]
queries = ["InterferenceQuery"]
graph_classes = ["Dag"]
validations = ["cheap", "full"]
reason = "InterferenceQuery has no native refuter suite; ATE refuters do not apply."

[[closed]]
queries = ["ConditionalEffect"]
graph_classes = ["Admg"]
reason = "ConditionalEffect on Admg has no compile arm; Dag, Cpdag, and Pag are licensed."

[[closed]]
queries = ["PathSpecificEffect"]
graph_classes = ["Admg", "Pag"]
structures = ["explicit"]
reason = "Path-specific queries execute only on a supplied static Dag; a directly supplied Admg/Pag hits the same static-Dag requirement (accepted/graph-posterior Admg and Pag are already refused above)."

[[closed]]
queries = ["InterventionalDistribution"]
graph_classes = ["Pag"]
structures = ["explicit"]
reason = "Path and distribution queries execute only on a supplied static Dag or Admg; a directly supplied Pag hits the static-Dag/Admg requirement."

[[closed]]
queries = ["InterventionalDistribution"]
graph_classes = ["Admg"]
structures = ["explicit"]
validations = ["cheap", "full"]
reason = "ADMG InterventionalDistribution is licensed at validation none; cheap/full remain Dag-only."

[[closed]]
queries = ["TemporalMediationEffect"]
graph_classes = ["TemporalPag"]
reason = "Latent-confounded TemporalPag natural mediation is outside the existing cross-world identification theory; DAG-completion engineering cannot license it."

[[closed]]
queries = [
  "AverageDerivative",
  "Counterfactual",
  "DirectionalDerivative",
  "Elasticity",
  "InterventionalDistribution",
  "MediationEffect",
  "PathSpecificEffect",
  "PointDerivative",
  "ResponseJacobian",
  "SemiElasticity",
  "TransportQuery",
  "InterferenceQuery",
]
graph_classes = ["Dag", "Admg", "Cpdag", "Pag"]
structures = ["graph_posterior"]
inferences = ["Frequentist"]
reason = "Frequentist graph-posterior mixing on DAG atoms is licensed for AverageEffect, ConditionalEffect, and static ResponseCurve/one-coordinate InterventionResponse. The listed static families remain unsupported."

[[closed]]
queries = ["TemporalMediationEffect"]
graph_classes = ["TemporalPag"]
structures = ["graph_posterior"]
inferences = ["Frequentist", "Bayesian"]
reason = "Latent-confounded TemporalPag natural mediation is outside the existing cross-world identification theory; TemporalPag graph-posterior atoms are not mixed into a mediation envelope."

# -- 2026-08-19 audit. The following reasons cover cells probed end-to-end
# (Study::build/run) and confirmed either to refuse at compile (an
# identifier/estimator pair the strategy table rejects) or to silently ignore
# the claimed inference mode. Former running exceptions were fully disposed in
# 0.9; parity/support_allowlist.toml is now required to remain empty.

# -- 0.9.0: n/a re-file (contract / identifier gap, not typed impossibility).

# -- 0.9.0: named reasons for refused families that previously had no reason row.

[[closed]]
queries = ["PathSpecificEffect", "InterventionalDistribution"]
graph_classes = ["Cpdag"]
structures = ["explicit"]
reason = "Path-specific queries execute only on a supplied static Dag and distribution queries only on a supplied static Dag or Admg; a directly supplied Cpdag hits that requirement (Admg/Pag explicit already named above)."

[[closed]]
queries = ["PathSpecificEffect", "InterventionalDistribution"]
graph_classes = ["Cpdag", "Admg", "Pag"]
validations = ["cheap", "full"]
reason = "Query-native functional validation requires a Dag."


# -- 1.3.0: remaining Dag coordinates after licensing Frequentist staged cells.

[[closed]]
queries = [
  "AverageDerivative",
  "DirectionalDerivative",
  "Elasticity",
  "PointDerivative",
  "ResponseJacobian",
  "SemiElasticity",
]
graph_classes = ["Dag"]
structures = ["graph_posterior"]
inferences = ["Bayesian"]
reason = "Graph-posterior derivative mixtures are not staged; the licensed derivative cells are Frequentist or Bayesian explicit or accepted Dag at validation none."

[[closed]]
queries = ["MediationEffect"]
graph_classes = ["Dag"]
structures = ["graph_posterior"]
inferences = ["Bayesian"]
reason = "Graph-posterior mediation mixtures are not staged; the licensed MediationEffect cells are Frequentist or Bayesian explicit or accepted Dag."

[[closed]]
queries = ["Counterfactual"]
graph_classes = ["Dag"]
structures = ["graph_posterior"]
reason = "Staged counterfactuals require a supplied Dag; graph-posterior structures are refused."

[[closed]]
queries = ["Counterfactual"]
graph_classes = ["Dag"]
validations = ["cheap", "full"]
reason = "Counterfactual cheap/full are not licensed; there is no native ITE refuter suite and ATE refuters do not apply."

# -- 1.10.1: named remaining graph-posterior response refusals. These stay
# refused; the rows are reasons, not licenses.

[[closed]]
queries = ["ResponseCurve"]
graph_classes = ["Admg", "Cpdag", "Pag"]
structures = ["graph_posterior"]
validations = ["cheap", "full"]
reason = "Graph-posterior ResponseCurve on Admg/Cpdag/Pag is licensed at validation none. cheap/full name the ATE-shaped scalar refuter suite; the curve remains function-valued or atom-mixed with no licensed scalar-refuter state."

[[closed]]
queries = ["InterventionResponse"]
graph_classes = ["Cpdag", "Pag"]
structures = ["graph_posterior"]
inferences = ["Bayesian"]
validations = ["cheap", "full"]
reason = "Bayesian graph-posterior InterventionResponse on Cpdag/Pag is licensed at validation none. cheap/full attach the Frequentist plugin-level suite; there is no Bayesian class-IR refuter path on a static Cpdag/Pag posterior."
