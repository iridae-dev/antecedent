# Status: pending | in_progress | done

library = "antecedent"

[[capabilities]]
id = "release.parity_closure"
status = "done"
notes = "Shipped pinned baseline rows are done across inventories; no waiver vocabulary; required 1.0 chapters closed"

[[capabilities]]
id = "release.graph_dot_json"
status = "done"
notes = "DOT + JSON + GML + NetworkX DAG interchange in antecedent-io"

[[capabilities]]
id = "release.artifact_schema"
status = "done"
notes = "Format 0.5 stable (antecedent_io::STABLE_FORMAT); migrates from 0.1, 0.2, 0.3, and 0.4; migrate registry + golden fixtures. 1.10 composition: validate_result_* in crates/antecedent-io/src/analysis_result_artifact.rs binds enclosing query, horizon, and mass. Parent evidence stays artifact-schema, not estimator truth."

[[capabilities]]
id = "release.wheel_matrix"
status = "done"
notes = "CPython 3.11–3.14 × Linux x86_64/aarch64, macOS arm64, Windows x86_64 wheels in CI"

[[capabilities]]
id = "release.conformance_docs"
status = "done"
notes = "docs/conformance generated from conformance/"

[[capabilities]]
id = "release.hot_path_baselines"
status = "done"
notes = "docs/hot_paths.md + benches/baselines owners/contracts"

[[capabilities]]
id = "release.security_review"
status = "done"
notes = "unsafe/license/dependency review + cargo-deny"

[[capabilities]]
id = "release.artifact_mmap_stream_skip"
status = "done"
notes = "ArtifactReader/MappedArtifactReader skip+mmap; SectionBytes Arc; Arrow Never-compress; posterior meta-only"

[[capabilities]]
id = "release.ci_required_jobs"
status = "done"
notes = "Job ids from .github/workflows/ci.yml that must succeed on the candidate SHA"
required_jobs = ["rust", "gates", "python-lint", "python-wheels"]
