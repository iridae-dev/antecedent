feature_id = "estimate.dml"
implementation_crate = "antecedent-estimate"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "Double/debiased machine learning for treatment and structural parameters", doi = "10.1111/ectj.12097", sections = ["partially linear model", "AIPW / Neyman-orthogonal score"] },
]

implementation_deviations = [
 "not an EconML clone: two scores only (binary AIPW and Robinson PLR), no 12-estimator catalog",
 "Robinson PLR is an ATE under a constant conditional effect; this restriction is recorded on the result",
 "learners never choose the adjustment set; identification stays upstream",
]

upstream_implementations_observed = [
 { project = "econml", exposure = "black-box comparison only", notes = "native DML; no EconML wrapper" },
]

test_sources = [
 "independently generated synthetic confounded SCM with known ATE",
]
