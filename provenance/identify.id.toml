feature_id = "identify.id"
implementation_crate = "antecedent-identify"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "Identification of Joint Interventional Distributions in Recursive Semi-Markovian Causal Models", sections = ["Algorithm ID"] },
]

upstream_implementations_observed = [
 { project = "dowhy", exposure = "black-box comparison only" },
]

test_sources = [
 "clean-room Shpitser-Pearl ID recursion oracle (conformance/identify/id_hedge)",
 # Recorded, NOT exercised. These three DoWhy 0.14 fixtures are frozen oracles for
 # the pending general ID / ID-IDC work; no test loads them, so no black-box
 # comparison currently runs. Listing them as an active comparison claimed evidence
 # that does not exist. Restore the comparison wording only when a test consumes them.
 "recorded-but-unexercised DoWhy 0.14 identify() oracles, pending general ID (conformance/identify/general_id_backdoor_chain, conformance/identify/general_id_frontdoor, conformance/identify/general_id_hedge)",
 "inline unit tests (crates/antecedent-identify/src/id.rs)",
]
