feature_id = "identify.id"
implementation_crate = "antecedent-identify"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

implementation_deviations = [
 "Lines 6 and 7 applied to a line-7 C-factor that line 2 has since marginalized form each conditional P'(v_i | v_pi^(i-1)) from its definition as a ratio of marginals of P' (numerator summed over the later variables, denominator additionally over v_i) rather than the paper's product notation; when no marginalization intervened the frozen observational factor is reused unchanged. Completeness is evidenced by exhaustive enumeration on <=4-node ADMGs with single treatment and outcome plus a seeded sample of 5-6 node joint queries, not proved for the implementation.",
 "Queries with an outcome that is also intervened on, or with no outcome, are refused before the recursion; the paper leaves them undefined. The former 'V\\X empty' fallback, which is not a published line, is now an unreachable invariant error.",
 "The functional keeps line-3 variables that line 7 later fixes outside S' as free variables (the napkin's z); it is not closed over them.",
 "Supports unconditional Distribution and AverageEffect queries with hard Set interventions, plus only constant interventions reducible to Set; conditional queries are delegated to IDC.",
 "Emitted products and SumOut expressions rely on enumerable variable domains and do not provide continuous-domain integral functionals.",
]

papers = [
 { title = "Identification of Joint Interventional Distributions in Recursive Semi-Markovian Causal Models", sections = ["Algorithm ID"] },
]

upstream_implementations_observed = [
 { project = "dowhy", exposure = "black-box comparison only" },
]

test_sources = [
 "frozen clean-room ID status cases for back-door, front-door, and hedge graphs (conformance/identify/id_hedge)",
 "consumed DoWhy 0.14 identify() status/estimand-family oracles (conformance/identify/general_id_backdoor_chain, conformance/identify/general_id_frontdoor, conformance/identify/general_id_hedge)",
 "inline unit tests (crates/antecedent-identify/src/id.rs)",
 "hand-derived worked examples: napkin, nested napkin, front-door, confounded-mediator hedge, bow arc, joint-treatment hedge (conformance/identify/id_completeness)",
 "exact enumeration of latent binary SCMs: all 49544 single-treatment single-outcome queries on 2-4 node ADMGs and 1500 seeded 5-6 node joint queries, each identified-and-numerically-correct or refuted by a definition-checked hedge (crates/antecedent-identify/tests/id_completeness.rs)",
 "conformance/estimate/admg_frontdoor_functional (frozen binary front-door law on the general_id_frontdoor ADMG: pins the FunctionalEffect number returned after general ID, on fresh and prepared paths)",
]
