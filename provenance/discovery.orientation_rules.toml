feature_id = "discovery.orientation_rules"
implementation_crate = "antecedent-discovery"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "On the completeness of orientation rules for causal discovery in the presence of latent confounders and selection bias", doi = "10.1016/j.artint.2008.08.001", sections = ["R1-R4 (collider propagation, R4 discriminating paths)", "R8-R10 (definite-path / uncovered-path rules, unprimed)"] },
 { title = "High-recall causal discovery for autocorrelated time series with latent confounders", doi = "10.48550/arXiv.2007.01884", sections = ["R3', R9', R10' generalizations (weakly-minimal separating-set requirement on middle colliders); NOT present in Zhang 2008"] },
]

implementation_deviations = [
 "The static FCI rule set omits Zhang R5-R7, so it is not the complete orientation rule set when selection-bias tail-tail structures are possible.",
 "Static FCI and RFCI reuse LPCMCI-primed R9 and R10: R9 adds a weakly-minimal-sepset requirement and R10 requires globally node-disjoint uncovered paths, both stricter than Zhang's unprimed static rules.",
 "R4 discriminating paths are capped at 64 paths and length 8; R9 and R10 uncovered-path searches are capped at 8 paths and length 8, with fail-closed budget errors.",
 "Contradictory orientations are represented by implementation-specific conflict endpoints rather than a paper-defined PAG mark.",
]

upstream_implementations_observed = []

test_sources = [
 "crates/antecedent-discovery/src/rule_scheduling_tests.rs",
 "crates/antecedent-discovery/src/discriminating_paths.rs unit tests",
 "conformance/discovery/rfci (clean-room motif expectations)",
 "conformance/discovery/lpcmci_matrix (Tigramite temporal skeleton only; PAG endpoints and middle marks are not compared)",
]
