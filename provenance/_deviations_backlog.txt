# Deviations-field backlog — frozen 2026-08-18.
#
# Every paper-backed scientific record was re-read in the 0.9 audit and now
# carries `implementation_deviations` (possibly `[]`). The records below are
# grandfathered engineering/folklore records with `papers = []`; they remain
# outside that paper-comparison audit. New records still cannot join this list.
#
# One id per line. Each id has the required one-line grandfather explanation.

# Grandfathered: papers = []; structure-attribution engineering, no cited procedure to re-read.
attribution.structure
# Grandfathered: papers = []; original design heuristic, no cited procedure to re-read.
design.effect_width_reduction
# Grandfathered: papers = []; calibration plumbing, no cited procedure to re-read.
estimate.calibration_coverage
# Grandfathered: papers = []; graph-unfolding engineering, no cited procedure to re-read.
graph.unfold
# Grandfathered: papers = []; identifier dispatch engineering, no cited procedure to re-read.
identify.auto
# Grandfathered: papers = []; result-envelope engineering, no cited procedure to re-read.
identify.envelope
# Grandfathered: papers = []; intervention support policy, no cited procedure to re-read.
identify.intervention_support
# Grandfathered: papers = []; incremental OLS engineering, no cited procedure to re-read.
state.incremental_ols
# Grandfathered: papers = []; rolling diagnostic engineering, no cited procedure to re-read.
state.rolling_mechanism_diag
# Grandfathered: papers = []; graph-refutation heuristic, no cited procedure to re-read.
validate.graph_refute
# Grandfathered: papers = []; robustness-refuter folklore, no cited procedure to re-read.
validate.robustness_refuters
