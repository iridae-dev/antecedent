feature_id = "discovery.pcmci"
implementation_crate = "antecedent-discovery"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "Detecting and quantifying causal associations in large nonlinear time series datasets", doi = "10.1126/sciadv.aau4996", sections = ["PCMCI algorithm"] },
]

implementation_deviations = [
 "This implementation refuses min_lag = 0 and delegates contemporaneous discovery to PCMCI+, rather than supporting PCMCI calls that include lag-zero associations.",
 "PC1 conditioning depth defaults to 3, and each level tests only the strongest conditioning set, matching Tigramite max_combinations = 1 rather than exhaustive subset search.",
 "MCI conditioning is subject to a 32-column hard limit and fails closed when the full parent conditioning set does not fit.",
]

upstream_implementations_observed = [
 { project = "tigramite", exposure = "black-box comparison only" },
]

test_sources = [
 "independently generated synthetic lagged VAR / SCM with known parent",
 "black-box optional comparison against pinned Tigramite baseline (conformance/discovery/pcmci_lag1)",
]
