feature_id = "ci.gpdc"
implementation_crate = "antecedent-stats"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "Measuring and Testing Dependence by Correlation of Distances", doi = "10.1214/009053607000000505", sections = ["population and empirical distance covariance and distance correlation"] },
]

implementation_deviations = [
 "Applies fixed-hyperparameter RBF kernel-ridge residualization (each Z column standardised first) before the cited biased sample distance correlation; the cited distance-correlation paper does not specify this GPDC construction. The distance correlation is evaluated in O(n log n) time and O(n) memory (sorting / Fenwick identities), including every permutation replicate; conditional inputs above 4000 rows are refused because the dense kernel factorization is O(n^2) memory, and a constant or fully Z-explained series is refused rather than reported as p = 1.",
]

upstream_implementations_observed = [
 { project = "tigramite", exposure = "black-box comparison only" },
]

test_sources = [
 "crates/antecedent-stats/src/ci/advanced.rs (gpdc_runs and gpdc_permutation_pvalue_separates_dependence unit tests)",
 "conformance/discovery/ci_stats (gpdc_dep / gpdc_ind, black-box comparison against pinned Tigramite baseline)",
 "conformance/discovery/advanced_ci (gpdc_cases, black-box comparison against pinned Tigramite baseline)",
]
