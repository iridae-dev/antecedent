feature_id = "identify.envelope"
implementation_crate = "antecedent-identify"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

# IdentificationEnvelope is a generic result-aggregation data structure (weighted
# per-graph cases plus preserved unidentified mass) used by generalized adjustment
# (see identify.generalized_adjustment.toml, which cites the graphical criterion
# this envelope aggregates results over). The envelope itself is original
# engineering, not a distinct published algorithm.
papers = []

upstream_implementations_observed = []

test_sources = [
 "inline unit tests (crates/antecedent-identify/src/envelope.rs)",
 "IdentificationEnvelope unidentified mass (conformance/identify/generalized_adjustment)",
]
