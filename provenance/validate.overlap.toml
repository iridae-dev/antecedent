feature_id = "validate.overlap"
implementation_crate = "antecedent-validate"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
 { title = "Dealing with Limited Overlap in Estimation of Average Treatment Effects", doi = "10.1093/biomet/asn055", sections = ["Propensity trimming rule (common-support band)"] },
 { title = "Survey Sampling", sections = ["Effective sample size (design effect)"] },
]

implementation_deviations = [
 "Combines two repository diagnostics: an eps=0.05 propensity-range plus ESS-fraction gate and a separate fixed [0.1, 0.9] retained-support rule.",
 "Fits a diagnostic-only logistic propensity when the original estimator has no overlap report.",
 "The Crump-style rule checks retained mass but does not derive the paper's sample-dependent optimal trimming threshold or re-estimate an effect on the trimmed target population.",
 "The default ESS and retained-fraction pass thresholds are repository policy.",
]

upstream_implementations_observed = []

test_sources = [
 "crates/antecedent-validate/src/overlap.rs unit tests",
 "crates/antecedent-validate/src/overlap_rule.rs unit tests",
 "conformance/validate/overlap_graph_refutation",
]
