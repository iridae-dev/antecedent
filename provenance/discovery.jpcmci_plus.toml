feature_id = "discovery.jpcmci_plus"
implementation_crate = "antecedent-discovery"
source_translation = false
copied_code = false
copied_comments = false
copied_tests = false

papers = [
  { title = "Causal discovery for time series from multiple datasets with latent contexts", doi = "10.48550/arXiv.2306.12896", sections = ["J-PCMCI+", "dummy variables", "link assumptions"] },
]

implementation_deviations = [
  "The default models each space-dummy one-hot column as a separate scalar causal node; the paper-faithful single logical multivariate dummy is optional, and time dummies are disabled by default.",
  "All four search phases inherit the default conditioning-depth limit of 3 and the 30-condition or 32-column MCI hard limit.",
  "Orientation reuses PCMCI+'s majority-collider implementation and adds contemporaneous Meek R4 beyond the pinned Tigramite J-PCMCI+ stack.",
]

upstream_implementations_observed = [
  { project = "tigramite", exposure = "black-box comparison only", notes = "Equality evidence is system-only and excludes observed context and dummy nodes" },
]

test_sources = [
  "crates/antecedent-discovery/src/jpcmci_plus.rs unit tests (jpcmci_plus_two_env_toy, observed_context_enters_ci, multivariate_space_dummy_uses_single_logical_node)",
  "crates/antecedent-discovery/tests/jpcmci_plus_oracle_matrix.rs (Tigramite canonical temporal skeleton equality with context and dummy nodes disabled)",
  "crates/antecedent-discovery/tests/discovery_jpcmci_plus_two_env_edges.rs (Tigramite system-only edge equality with the space dummy disabled)",
  "conformance/discovery/jpcmci_plus_two_env (library structure pin; not full Tigramite context/dummy equality)",
  "conformance/discovery/jpcmci_plus_two_env_edges (Tigramite system-only edges without context or dummy nodes)",
  "conformance/discovery/jpcmci_plus_matrix (Tigramite canonical temporal skeleton with context and dummy nodes disabled)",
  "conformance/discovery/jpcmci_plus_two_env_space_dummy_mv (library-only multivariate-dummy structure and diagnostic pin)",
]
