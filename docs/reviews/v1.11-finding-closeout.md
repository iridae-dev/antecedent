# Antecedent 1.11 finding closeout

Date: 2026-09-19
Base: `6a41568f` on `1.11` (finding commits `3c71b5c8`, `1c048dae`, `6a41568f`)
Disposition: **do not close 1.x** until every P1 and P2 finding has a consuming
regression and the attestation gate is reassessed under the repaired evidence
rule.

The 1.11 review found eleven defects. Closures below are the conservative
option in each review fork. Review probes under `docs/reviews/v1.11-review/`
are diagnostics; consuming tests in CI are the gate.

## Closures

| Id | Closure | Consuming test |
| --- | --- | --- |
| R1 | Every coverage record carries `core`. Replay waivers remain for reviewed non-numeric diffs. | Facet self-test plus mutation of OLS / bootstrap / conjugate / RNG / toolchain paths |
| R2 | Reject instruments that are descendants of treatment. No Chan–Kuroki descendant-IV constructions. | Four-node descendant graph on the direct identifier and `AutoIdentifier` |
| R3 | Conjugate Gram keys `X` and weights by content digest, not pointer address. | Allocator-reuse fresh ≡ reused MAP (skip the address-reuse assertion when the allocator does not recycle) |
| R4 | Gaussian BIC uses the actual residual variance. Exact-zero / non-finite SSE is a degenerate outcome, not a `1e-12` floor. Local fit uses equilibrated QR. | Score-difference and posterior-weight invariance under outcome and predictor rescales |
| R5 | `minimal_only` stops at a valid empty set. Skip known supersets before d-separation. `max_examinations` is a work budget distinct from `max_results`. Exhaustion is incomplete search. | Wide sparse graph examines one candidate; a hard confounded graph reports budget exhaustion honestly |
| R6 | Wald refuses a nonempty `adjustment_set`. Callers use 2SLS. | Eight-row covariate example: Wald refuses; 2SLS recovers 2 |
| R7 | kNN workspace identity is a full content digest of the involved columns. | Unsampled-row mutation, changed conditioning column, fresh ≡ reused |
| R8 | Incremental OLS residual variance uses a Welford joint scatter. Unrecoverable numeric state is `None`, not a clipped zero. | Intercept-only `1e12 + [-2,-1,0,1,2]` reports 2.5 |
| R9 | `ArrowCColumn` fields are private. `unsafe from_ffi` carries the Arrow importer contract; `from_arrow_array` is the safe constructor. | Type-level: a mismatched pair cannot reach `from_ffi` through entirely safe public calls |
| R10 | Safe `open_path` is an owned snapshot. Zero-copy mmap is `unsafe open_path_mapped` with a lifetime-wide immutability contract. | Snapshot survives replacement; mapped path documents the contract |
| R11 | `CalibrationScopeWire.row_count` is the analysis sample. Snapshot `row_count` stays input identity. | 100 complete rows vs 100+400 / 100+900 missing keep the same label and point/SE; temporal lag-aligned and matching routes; artifact roundtrip |

## Evidence rule

`scripts/calibration_facets.py` always adds `core`. Drift already mapped
unmapped shared files to `core`; excluding the category made the intersection
empty, so OLS / bootstrap / conjugate / RNG / toolchain edits invalidated
0/587. That is repaired. Core-touching numeric commits owe remesure. Do not
rename claims, widen tolerances, or weaken the support matrix to go green.

## Out of scope unless a repair forces it

Sibling practitioner suite rerun, CodeQL, wheel isolation, the full statistical
calibration campaign as a release certificate, and exact-candidate release
approval. A fresh `cargo deny check` against a fetched advisory database passed
on 2026-09-19 (ignored `RUSTSEC-2024-0436` as in `deny.toml`).
