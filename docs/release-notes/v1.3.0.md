# Antecedent 1.3.0

Existing query kinds on the staged handle.

Six derivative kinds now execute through identify → prepare → estimate on
explicit/accepted Frequentist DAGs. Static natural mediation adds native
cheap/full validation. Explicit-DAG unit counterfactuals refit GCM mechanisms
and preserve nonzero control levels. Conditional right/left censoring uses Cox
IPCW with executing R survival parity.

See the [evidence ledger](../v1.3-evidence.md) and
[observation pair contract](../observation-contract.md) for model restrictions,
consuming fixtures and uncertainty availability. No transport/interference
`analyze` cells or 2.0 sensitivity/sID scope are added.

<!-- generated:support-matrix:licensed:begin -->
134 licensed of 1342 meaningful cells (1052 n/a typed impossibilities are not a coverage gap). 1155 refused with a reason on file; 53 refused without a reason; 0 active `allowed_unlicensed` compatibility entries.

- `AverageDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `AverageEffect` × `Dag` / `Admg` / `Pag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `AverageEffect` × `Dag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `AverageEffect` × `Pag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `ConditionalEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `ConditionalEffect` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `Counterfactual` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `DirectionalDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `Elasticity` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `InterventionalDistribution` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `InterventionResponse` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `MediationEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `PathSpecificEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `PointDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `PulseEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `PulseEffect` × `TemporalDag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `ResponseCurve` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `ResponseCurve` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `ResponseJacobian` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `SemiElasticity` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `SustainedEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `SustainedEffect` × `TemporalDag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `TemporalMediationEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `TemporalMediationEffect` × `TemporalDag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
<!-- generated:support-matrix:licensed:end -->

## Explicit refusals

The [authoritative support matrix](../support-matrix.md) governs every cell.
Derivative cheap/full are n/a. Partial-graph and Bayesian derivatives, weighted
Riesz representers, oversized Jacobians, nested/temporal/accepted counterfactuals,
conditional delayed entry and interval/truncated response MLE remain refused.
Joint observation/curve uncertainty remains omitted.
