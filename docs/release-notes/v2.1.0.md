# Antecedent 2.1.0 release notes

2.1.0 is in development. This branch expands Bayesian estimator scope and the evidence behind its support claims. The [support matrix](../support-matrix.md) remains the authoritative coordinate license.

## Evidence and assumptions slice

- Single-source z-transport covers finite discrete diagrams of up to twelve observed and four controllable variables. A positive result is a checked TRz formula and evaluates from the joints that formula cites, including the registered four-variable surrogate and an admissible selection diagram. A line-11 certificate is structural in the declared controllable set: the same terminal is reached with an empty catalog as with the full experiment family. Two sources are searched separately; one identifying source is enough, both line-11 terminals are one obstruction, and cross-source combination is refused as `z_transport.multi_source_combination_not_searched`. Empirical cited tables publish an equal-tail percentile bootstrap by default, and the same tables also accept the empirical-support Bayesian bootstrap and the declared-state-space Dirichlet posterior. A published interval is pointwise, withholds when the bootstrap failure rule trips, and its reason is `estimator_grid_not_measured`; exact laws stay point-only, and that interval is not a coverage claim. The result stays sound and incomplete. Outcome-kernel sensitivity stays on the registered surrogate factorization.
- Checked transport proofs expose factor leaves, their source regimes, and binding failures. Immutable hypothetical catalog deltas let a planner test proposed evidence without adding it to a prepared analysis.
- A finite discrete outcome-kernel contamination model reports an exact assumption range, extremal witnesses, and decision tipping point. Its range is not a confidence interval.
- A finite candidate planner tests hypothetical evidence deltas, ranks only checked sufficient additions by declared cost, and emits a replayable proposal with a base-catalog digest. Declared feasibility is supplied by the planner caller; a cost rank is not a success probability.
- A fixed three-node Markovian mediation DAG has a point-only natural direct-effect route with coupled exogenous draws and typed incompatible-world refusals. See the [counterfactual coverage matrix](../../parity/counterfactual_coverage.md).
- A CPU baseline measures end-to-end cross-fitted neural nuisance fitting, including fold planning. No GPU provider is introduced.
- The [transport coverage matrix](../../parity/transport_coverage.md) and [calibration backlog](../../parity/calibration_backlog.md) expose licensed and refused scope and unmeasured intervals.

## Bayesian estimator changes

- Bayesian conditional effects accept multiple declared modifiers and evaluate interactions within each posterior draw. Non-Gaussian conditional and response levels are evaluated through their inverse link on the outcome scale.
- Response curves can report a simultaneous band from the same joint posterior draws on a fixed grid.
- Bayesian anomaly and change attribution preserve shared row-law draws across components. Trial-to-target transport uses a Bayesian bootstrap over observed trial rows, conditional on supplied selection and treatment probabilities. Finite-network interference has a separate Gaussian model-based posterior route.
- CPDAG and PAG graph-posterior intervention responses run cheap and full validation while retaining graph mass, unresolved atoms, and atom-level failures.
- Finite-discrete structural transport offers separate empirical-support bootstrap and declared-state-space Dirichlet law providers. An exact sparse-table Dirichlet moment check and public Python artifact lifecycle test exercise both providers. A published equal-tail interval is pointwise, shares one draw sequence across a treatment grid, and withholds that interval when the bootstrap failure rule trips. Its coverage reason remains `estimator_grid_not_measured` until the release calibration pass.
- `bayesian.basis.gcomp` fits a Gaussian quadratic basis with treatment interactions and a declared shrinkage prior for `AllObserved` ATE and row CATE. A synthetic 100-replicate nominal 90% ATE check covered 95/100; no registry coverage record is attached yet.
- `bayesian.robust_ate` refits cross-fitted weighted outcome and propensity nuisances under shared Bayesian-bootstrap row weights. Its interval is labeled a modular bootstrap pushforward. Four bounded 40-replicate scenarios test single-nuisance, joint misspecification, and weak-overlap behavior; nominal 90% interval coverage ranged from 77.5% to 90%, so a calibrated coverage claim is withheld.
- `rd.bayesian_local_linear` has a dedicated sharp RD model conditional on the declared bandwidth; a 300-replicate exact-law check covered 95.7% at nominal 95%. `iv.bayesian_joint_linear` uses a joint Gaussian structural model with a fixed unit disturbance loading and known unit stage variances. Its 300-replicate exact-law interval check passes; the earlier free-loading control-function candidate covered 100% and is not licensed.

## Explicit refusals

The new licenses are limited to their stated outcome models, populations, exposure mappings, graph classes, and validation tiers. Unlisted coordinates and unsupported priors or target populations still refuse. See the [support matrix](../support-matrix.md#refusal-reasons) for coordinate-level reasons.

## Licensed support matrix

<!-- generated:support-matrix:licensed:begin -->
472 licensed of 1475 meaningful cells (2089 n/a typed impossibilities are not a coverage gap). 1003 refused with a reason on file; 0 refused without a reason; 0 active `allowed_unlicensed` compatibility entries.

Interval calibration of the 472 licensed cells: 295 cite coverage records; 172 across 92 distinct coordinates have no coverage measurement for their estimator (`estimator_grid_not_measured`); 5 report no interval (`no_interval_reported`). A licensed cell therefore does not imply that its interval coverage was measured, and cited records do not by themselves make a result `calibrated` (see `result.calibration`).

- `AnomalyAttribution` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `AnomalyAttribution` × `Dag` × `explicit` × `Bayesian` × validation `none`
- `AverageDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `AverageDerivative` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `AverageEffect` × `Dag` / `Admg` / `Cpdag` / `Pag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `AverageEffect` × `CoDetermined` / `Unknown` × `explicit` × `Frequentist` × validation `none`
- `AverageEffect` × `Dag` / `Admg` / `Cpdag` / `Pag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `ChangeAttribution` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `ChangeAttribution` × `Dag` × `explicit` × `Bayesian` × validation `none`
- `ConditionalEffect` × `Dag` / `Cpdag` / `Pag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `ConditionalEffect` × `Dag` / `Cpdag` / `Pag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `Counterfactual` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `Counterfactual` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `DirectionalDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `DirectionalDerivative` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `Elasticity` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `Elasticity` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `InterventionalDistribution` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `InterventionalDistribution` × `Admg` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `InterventionalDistribution` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `InterventionalDistribution` × `Admg` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `Dag` / `Admg` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `InterventionResponse` × `Cpdag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `Cpdag` × `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Frequentist` × validation `none`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Frequentist` × validation `cheap`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Frequentist` × validation `full`
- `InterventionResponse` × `Pag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `Pag` × `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Frequentist` × validation `none`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Frequentist` × validation `cheap`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Frequentist` × validation `full`
- `InterventionResponse` × `TemporalDag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalDag` × `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Frequentist` × validation `cheap`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Frequentist` × validation `full`
- `InterventionResponse` × `TemporalCpdag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Frequentist` × validation `cheap`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Frequentist` × validation `full`
- `InterventionResponse` × `TemporalPag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalPag` × `accepted` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Frequentist` × validation `none`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Frequentist` × validation `cheap`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Frequentist` × validation `full`
- `InterventionResponse` × `CoDetermined` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `Dag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `Admg` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `InterventionResponse` × `Cpdag` × `explicit` × `Bayesian` × validation `none`
- `InterventionResponse` × `Cpdag` × `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Bayesian` × validation `cheap`
- `InterventionResponse` × `Cpdag` × `graph_posterior` × `Bayesian` × validation `full`
- `InterventionResponse` × `Pag` × `explicit` × `Bayesian` × validation `none`
- `InterventionResponse` × `Pag` × `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Bayesian` × validation `cheap`
- `InterventionResponse` × `Pag` × `graph_posterior` × `Bayesian` × validation `full`
- `InterventionResponse` × `TemporalDag` × `explicit` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalDag` × `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Bayesian` × validation `cheap`
- `InterventionResponse` × `TemporalDag` × `graph_posterior` × `Bayesian` × validation `full`
- `InterventionResponse` × `TemporalCpdag` × `explicit` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Bayesian` × validation `cheap`
- `InterventionResponse` × `TemporalCpdag` × `graph_posterior` × `Bayesian` × validation `full`
- `InterventionResponse` × `TemporalPag` × `explicit` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalPag` × `accepted` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Bayesian` × validation `none`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Bayesian` × validation `cheap`
- `InterventionResponse` × `TemporalPag` × `graph_posterior` × `Bayesian` × validation `full`
- `MediationEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `MediationEffect` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `NestedCounterfactualEffect` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `PathSpecificEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `PathSpecificEffect` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `PointDerivative` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `PointDerivative` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `PulseEffect` × `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `PulseEffect` × `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `ResponseCurve` × `Dag` / `Admg` / `Cpdag` / `Pag` / `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none`
- `ResponseCurve` × `Dag` / `Admg` / `Cpdag` / `Pag` / `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none`
- `ResponseJacobian` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `ResponseJacobian` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `SemiElasticity` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `SemiElasticity` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `SustainedEffect` × `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `SustainedEffect` × `TemporalDag` / `TemporalCpdag` / `TemporalPag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `TemporalMediationEffect` × `TemporalDag` / `TemporalCpdag` × `explicit` / `accepted` / `graph_posterior` × `Frequentist` × validation `none` / `cheap` / `full`
- `TemporalMediationEffect` × `TemporalDag` / `TemporalCpdag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `TransportQuery` × `Admg` × `explicit` × `Frequentist` × validation `none`
- `TransportQuery` × `Admg` × `explicit` × `Bayesian` × validation `none`
- `InterferenceQuery` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `InterferenceQuery` × `Dag` × `explicit` × `Bayesian` × validation `none`
<!-- generated:support-matrix:licensed:end -->
