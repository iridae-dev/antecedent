# Antecedent 0.6.0

0.5.2 was a performance pass. 0.6.0 is the **contract cut**: every public
sentence about what Antecedent will run is a machine-checked cell. This is
not a filled matrix. Workspace and Python package versions stay **0.5.2**
until the cut is accepted.

## The matrix is the license

The public license is the generated
[support matrix](../support-matrix.md)
([ADR 0020](../../adr/0020-support-matrix-and-prepared-workflow.md)).
`docs/capabilities.md` is inventory; it does not license a cell.

A cell is exactly one of **licensed**, **n/a**, or **refused**. Default is
refused. `pending` is illegal on a cell. `analyze` / `Study::run` is sugar
over identify → prepare → estimate; a combination that only works inside
`analyze` cannot be licensed.

## Licensed cells

The block below is generated from `parity/support_licensed.toml` by
`scripts/generate_support_matrix_docs.py`; the release gate fails if it
is stale. It is a draft state, not a freeze.

<!-- generated:support-matrix:licensed:begin -->
2394 cartesian cells: 16 licensed,
1218 n/a, 993 enforced refusals,
167 default-refused (still run until licensed or closed).

- `AverageEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `AverageEffect` × `Dag` × `explicit` / `accepted` × `Bayesian` × validation `none`
- `ConditionalEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `InterventionalDistribution` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `PathSpecificEffect` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `ResponseCurve` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none`
<!-- generated:support-matrix:licensed:end -->

Notes on the licensed set (hand-written; axes and counts above are
authoritative): `ConditionalEffect` runs the `ConditionalLinearAdjustment`
estimator on the prepared handle; `PathSpecificEffect` runs
`PathSpecificNatural` + `FunctionalEffect`; `InterventionalDistribution`
runs `GeneralId` + `FunctionalDistribution` (its known-truth fixture pins
identification, not the estimation layer); `InterventionResponse` runs
`response.intervention_gcomp` (its fixture pins a hard `Set` intervention
under a deterministic linear structural mean).

## Enforced refusals and n/a

`parity/support_closed.toml` fail-closes Counterfactual, MediationEffect,
and SustainedEffect (not on the staged handle); derivative queries (only
`ResponseCurve` on a Dag is staged); `ResponseCurve` and `InterventionResponse`
on Pag / Cpdag / Admg (the engine executes both only against a supplied
static Dag); `ConditionalEffect` on Pag / Cpdag / Admg (no compile arm exists
for those classes); and PathSpecificEffect / InterventionalDistribution on
`accepted` or `graph_posterior`, or on a directly supplied Admg / Pag (only
their `explicit` Dag cell is staged). `TemporalMediationEffect` is closed on
TemporalCpdag / TemporalPag (compilation only completes against a supplied
TemporalDag). Graph-posterior structures are closed under `Frequentist`
inference for every query (a graph posterior is a mixture over structures;
only Bayesian inference can combine per-graph effect draws into an envelope).
TransportQuery and InterferenceQuery are closed as analyze cells.

Graph-posterior × response is Option B: **n/a** (contrast-only). ATE ×
graph posterior stays as today (per-graph identify inside `execute()`, no
`identify_only`).

Stage transport and interference are not licensed `analyze` cells. The
implemented sID subset plus `NotCertified` is the transport contract.

Full details in the [CHANGELOG](../../CHANGELOG.md).
