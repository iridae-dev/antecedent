# Antecedent 0.9.0

0.9.0 closes the support matrix and brings prepared analyses to parity with
the licensed one-shot paths. Unsupported combinations no longer run under an
`allowed_unlicensed` escape hatch: they are classified as typed
not-applicabilities or refused before execution.

## Prepared analyses

`PreparedAnalysis` now covers PAG and ADMG ATE, graph-posterior ATE,
temporal pulse and sustained effects, DBN-posterior temporal effects, and
temporal mediation. Re-estimation preserves series semantics, and second-click
Bayesian refutation on a single-graph Dag includes prior and posterior
predictive checks. The multi-graph PAG envelope is the exception: it runs the
effect refuters on the mixture mean and records a `refute.bayesian.ppc.skipped`
diagnostic instead of a PPC suite.

Parity here is over which cells accept `prepare()`, not over caching. PAG
envelopes, bidirected ADMGs, and graph posteriors accept a prepared study but
cache no identification: they re-identify on every click, and each licensed row
says so in its `limitations`. Caching them is 1.x optimization work, not a
missing cell.

Prepared analyses reject identifier, estimator, prior-transfer, and mapping
options when the selected path cannot apply them rather than silently
discarding those options.

## Boundary correctness

Python distribution, path-specific, discovery, and temporal effect entry
points propagate requested refutation suites into the native analysis.
Function-valued response queries reject scalar suites as not applicable.
PAG, CPDAG, and ADMG analyses also validate graph variable names and order
against Arrow columns before estimation.

## Frozen public contracts

The Python root is frozen at 49 exports through 0.9. Fourteen additional
modules remain reachable but deliberately outside `antecedent.__all__`.
`TemporalCpdag` and `TemporalPag` stay on `antecedent.graph`; derivative query
types stay at the root so unsupported analyses return typed refusals rather
than disappearing from the API.

Durable artifact format 0.4 is the wire-format freeze for 1.0. Existing
migration and cross-language round-trip tests cover the implemented query and
result variants.

The 0.9 release invariant is zero active allowlist entries. The
`allowed_unlicensed` wire value remains decodable for compatibility, but new
allowlist rules fail the support gate.

## Graph-posterior scope

Licensed graph-posterior analysis is intentionally narrow:

- Bayesian `AverageEffect` envelopes over DAG atoms support validation
  `none`, `cheap`, and `full`.
- Bayesian pulse and single-step sustained envelopes over `TemporalDag` atoms
  support validation `none`; cheap/full are refused because the DBN path
  returns no refutations.
- Frequentist graph-posterior cells, response mixtures, and
  ADMG/CPDAG/PAG posterior atoms are refused.

Identification runs per atom on each prepared estimate click. Unidentified
atom mass is retained, and priors do not upgrade identification. The current
evidence contract is prepared-vs-fresh internal cross-checking, not a
known-truth pin for the mixture effect. Bayesian PAG ATE is a separate
completion/generalized-adjustment envelope, not a `graph_posterior` structure
cell.

For static graph-posterior ATE, cheap/full run the applicable effect refuters
on the mixture result. That path does not currently attach the single-graph
Bayesian predictive-check or prior-sensitivity outputs; `full` is therefore not
a claim of those Bayesian diagnostics. More generally, a licensed cell means
the recorded staged behavior and evidence contract hold. It does not verify the
cell's causal assumptions from the observed data or establish universal
frequentist/Bayesian calibration.

Path-specific and interventional-distribution cheap/full validation, and
temporal-mediation cheap/full validation, remain refused: those execution
paths return no query-native refutation suite.

## Explicit refusals

The [support matrix](../support-matrix.md) is authoritative; omission from the
licensed block below means refusal unless the coordinate is one of the matrix's
typed n/a rules. The main scientific refusals in this cut are:

- `Counterfactual`, static `MediationEffect`, `AverageDerivative`,
  `PointDerivative`, `Elasticity`, `SemiElasticity`, `DirectionalDerivative`,
  and `ResponseJacobian` have no licensed analysis cell. They remain importable
  so callers get a typed refusal rather than an absent API.
- `Cpdag` has no licensed analysis cell. Incomplete `TemporalCpdag` and
  `TemporalPag` analysis is likewise refused; completing one to a
  `TemporalDag` is a different coordinate.
- Bayesian response curves/intervention responses, response mixtures over
  graph posteriors, Frequentist graph-posterior analysis, and non-DAG posterior
  atoms (`Admg`, `Cpdag`, or `Pag`) are refused. `TemporalDag` DBN-posterior
  pulse/single-step-sustained validation-`none` cells are the documented
  temporal exception.
- Accepted or graph-posterior path-specific and interventional-distribution
  analysis is refused; their only licensed cells are explicit-DAG,
  Frequentist, validation-`none`.
- Bayesian temporal mediation and temporal-mediation cheap/full validation are
  refused. Multi-step sustained windows remain estimator-refused; the licensed
  sustained form is single-step.

## Licensed cells

The block below is generated from `parity/support_licensed.toml` by
`scripts/generate_support_matrix_docs.py`; the release gate fails if it is
stale.

<!-- generated:support-matrix:licensed:begin -->
77 licensed of 1406 meaningful cells (988 n/a typed impossibilities are not a coverage gap). 1329 refused with a reason on file; 0 refused without a reason; 0 active `allowed_unlicensed` compatibility entries.

- `AverageEffect` × `Dag` / `Admg` / `Pag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `AverageEffect` × `Dag` × `explicit` / `accepted` / `graph_posterior` × `Bayesian` × validation `none` / `cheap` / `full`
- `AverageEffect` × `Pag` × `explicit` / `accepted` × `Bayesian` × validation `none` / `cheap` / `full`
- `ConditionalEffect` × `Dag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `InterventionalDistribution` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `InterventionResponse` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `PathSpecificEffect` × `Dag` × `explicit` × `Frequentist` × validation `none`
- `PulseEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `PulseEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `none`
- `PulseEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `cheap`
- `PulseEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `full`
- `PulseEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `none`
- `PulseEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `cheap`
- `PulseEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `full`
- `PulseEffect` × `TemporalDag` × `graph_posterior` × `Bayesian` × validation `none`
- `ResponseCurve` × `Dag` / `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none`
- `SustainedEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none` / `cheap` / `full`
- `SustainedEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `none`
- `SustainedEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `cheap`
- `SustainedEffect` × `TemporalDag` × `explicit` × `Bayesian` × validation `full`
- `SustainedEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `none`
- `SustainedEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `cheap`
- `SustainedEffect` × `TemporalDag` × `accepted` × `Bayesian` × validation `full`
- `SustainedEffect` × `TemporalDag` × `graph_posterior` × `Bayesian` × validation `none`
- `TemporalMediationEffect` × `TemporalDag` × `explicit` / `accepted` × `Frequentist` × validation `none`
<!-- generated:support-matrix:licensed:end -->

Full details are in the [CHANGELOG](../../CHANGELOG.md).
