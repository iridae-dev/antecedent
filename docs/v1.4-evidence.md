# 1.4 evidence ledger

The first licensed `Cpdag` analysis cells. Runtime class stays `Cpdag`. MEC
DAG completions are envelope atoms, not relabeled `Dag` posteriors.

| Claim | Frozen numerical evidence | Consumer |
|---|---|---|
| CPDAG ATE envelope, explicit/accepted, Frequentist and Bayesian, `none`/`cheap`/`full` | `conformance/estimate/cpdag_ate_envelope` | Rust `cpdag_ate_numeric_pins`; Python `test_cpdag_ate_numeric_pins` |

The two-completion law is the same contingency table as the 1.1 PAG envelope
fixture, with `Z — T` instead of `Z o-o T`. Completions identify at `0.40`
(adjust for `Z`) and `0.52` (unadjusted). Equal weights pin Frequentist ATE
`0.46`. The seeded conjugate envelope mean (`0.4615641945101853`) is an
output pin.

Prepared handles freeze and reuse the MEC envelope; estimate and same-schema
refresh clicks emit `exec.identify.cached`. Completing the CPDAG to a DAG
yourself is still the `Dag` cell.

| Class-aware `ResponseCurve` / `InterventionResponse` on `Cpdag` / `Pag`, explicit/accepted, Frequentist and Bayesian, `none` | `conformance/response/class_aware_envelope` | Rust `class_aware_response_numeric_pins`; Python `test_class_aware_response_numeric_pins` |

The response cells reuse the ATE envelope. Completions that adjust for `Z`
identify `E[Y|do(T=t)] = 0.20 + 0.40 t`; the unadjusted completion identifies
`0.14` and `0.66`. Mass-weighted intervention g-computation on the discrete
ATE table pins CPDAG `do(0)`/`do(1)` and contrast `0.46`, and PAG contrast
`0.44`. Kennedy-DR is singular on that binary treatment, so the curve pin
uses a continuous linear sibling and checks the two-point contrast against
AverageEffect on the same table, with a declared tolerance. The sidecar
min/max PAG curve is not the licensed number.

`Admg` response stays refused: licensed general-ID ATE has no curve plug-in.
Bayesian Cpdag/Pag response is the same envelope with `response.bayesian`.
Per-completion posterior draws are not mixed; the licensed number is the
identified-mass mean, disclosed by `estimate.envelope.response_posterior_not_mixed`.
The two-point intervention contrast is pinned against the CPDAG / PAG
Bayesian ATE fixtures (`0.4615641945101853` / `0.44161301501541206`).

| Class-aware `ConditionalEffect` on `Cpdag` / `Pag`, explicit/accepted, Frequentist and Bayesian, `none`/`cheap`/`full` | `conformance/estimate/cpdag_ate_envelope` | Rust `class_aware_conditional_numeric_pins`; Python `test_class_aware_conditional_numeric_pins` |

Conditional adjustment is checked in each completion: the modifier must be
pre-treatment, and the outcome-model conditioning set must block backdoor
paths. In the fixture the `T -> Z` mediator completion is unverified and
retains unidentified mass. The supported `Z -> T` completion yields `0.40`;
the aggregate status is `GraphDependent`. This is a sufficient check on the
selected adjustment sets, not a complete conditional identification search.
Runtime class stays `Cpdag` / `Pag`. `Admg` ConditionalEffect stays refused.

`identify()` returns the class envelope (or TemporalDag identification)
without data. Temporal adjustment names currently omit offsets, so the
EconML adapter refuses temporal results rather than exporting misaligned data.

| Pulse / single-step Sustained on `TemporalCpdag` / `TemporalPag`, explicit/accepted, Frequentist, `none`/`cheap`/`full` | `conformance/estimate/temporal_class_envelope` | Rust `temporal_class_envelope_numeric_pins`; Python `test_temporal_class_envelope_numeric_pins` |

The two-completion temporal law is `z` alternating, `t = 0.3 + 0.4 z + 0.05 sin(0.017 i)`, `y_i = 1 + 2 t_{i-1} + 0.5 z_{i-1}`. Contemporaneous `z — t` (CPDAG) or `z o-o t` (PAG) plus lagged `z → y` and `t → y`. Completions identify the true lagged adjustment and the unadjusted mediator orientation; equal weights pin Frequentist pulse ATE `2.6067949447664525`. Single-step Sustained matches Pulse. TemporalPag enumeration is restricted to DAG endpoint refinements, excludes latent-confounded MAG completions, and does not audit global PAG equivalence; its identification status is never promoted to class-wide point identification. Multi-step Sustained and Bayesian incomplete-class cells stay refused. Completing the graph yourself is still the `TemporalDag` cell.

| EconML adjustment-set handoff | Python `test_econml_handoff` | `antecedent.handoff.econml` |

The adapter is a typed refusal surface, not a numeric pin. Static backdoor ATE emits `W={z}`. Temporal estimands refuse because the
spec cannot preserve treatment and adjustment offsets. A fully-oriented Cpdag stays a Cpdag and
emits `W={z}` when the single completion is point-identified. Partial CPDAG
envelopes, front-door, and graph-posterior mixtures refuse.
Graph-posterior refusal uses `plan.structure_source`, then the algorithm-name
heuristic, then identification status.

| Frequentist graph-posterior ATE, `none`/`cheap`/`full` | `conformance/bayesian/known_truth_mixtures` | Rust `graph_posterior_ate_known_truth_mixture_frequentist`; Python `test_static_known_truth_mixture_frequentist` |

Same atoms as the 1.1 Bayesian mixture: unadjusted effect 3 (weight 0.5), Z-adjusted effect 2 (weight 0.3), reverse-causal `Y → T` unidentified (weight 0.2). The Frequentist aggregator reports `E[τ | identified] = 2.625` from mass-weighted `linear.adjustment.ate`, retains unidentified mass on `estimate.graph_posterior.envelope`, and publishes `GraphDependent` rather than the first atom's point-ID status. Cheap/full mix effect refuters across contributing atoms against the mixture effect. Multi-atom Frequentist SEs and response bands are unavailable: per-atom SEs do not specify the cross-fit sampling covariance, and averaging endpoints does not produce mixture quantiles. A single contributing atom retains its uncertainty. The legacy diagnostic `estimate.envelope.se_omits_between_atom_variance` explains this limitation. Frequentist DBN-posterior mixing stays 1.6. `AnalysisResult.plan.structure_source` is `graph_posterior` on this path.

## Verification

Licensed rows require `scripts/gate_support_matrix.sh` to pass.
