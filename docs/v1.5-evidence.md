# 1.5 review and evidence ledger

The three previously open 1.5 completion gaps — ConditionalEffect
per-arm CDF bands and tail support, shared prepared-batch designs, and
multi-atom PAG response inference — are implemented and pinned
below. This ledger records consuming evidence. It does not treat a
passing scalar test as a universal coverage guarantee.

## Corrections made during review

- Joint `cell.aipw` now estimates the requested intervention cell, including
  zero levels, instead of returning a 2×2 interaction for every query. Invalid
  levels and incomplete Set intervention bundles refuse.
- Weighted named cell contrasts use the weighted summary and covariance.
  Named `interaction` on an exceedance grid is refused; request a single
  Exceedance threshold or a raw coefficient vector.
- Joint nuisance fitting uses a multinomial-logit likelihood. Empty training
  cells refuse instead of supplying zero outcome regressions and tiny invented
  propensities. `StudyBuilder::continuous_cell` is a named refuse: coarsening D
  onto a grid is not a controlled direct effect at a point (`do(D=d0)`). Point
  CDE is unlicensed.
- Target-local support checks every joint cell using original held-out
  propensities. Scores retain observed cells, outcomes, and row indexes through
  artifact round trips. Missing support evidence is not treated as success.
- Weighted covariance agrees with unweighted covariance for constant weights.
  Nonfinite scores and invalid covariance matrices refuse. Cross-fitting is no
  longer described as sufficient on its own for efficient inference.
- AIPW custom distributions require declared weight parents. Their estimate,
  influence sequence, and optional refitted bootstrap use the weighted target.
  Failures no longer silently fall back to an unrelated full-sample estimate.
- Prepared exceedance estimates use the new data's scores. Refresh commits
  retained data and scores together after successful execution. Explicit AIPW
  plans preserve configured propensity options and propagate score-build failures;
  iid score inference refuses trimming and dependent-row SE configurations.
  Other estimator plans no longer silently fit an additional AIPW estimator.
- Paths that cannot execute the requested outcome transform or full grid now
  refuse instead of returning a mean or silently reducing the grid to its first
  threshold. Frequentist `ConditionalEffect` grids (Dag/Cpdag/Pag) publish
  per-arm `F_a(c)` and 2K raw-score simultaneous bands with per-threshold
  tail support. Class-aware Cpdag/Pag grids mix per-arm IFs by frozen
  completion weight, the same rule as the mean CATE mix; a contributing atom
  without an aligned IF refuses rather than mixing covariance from a subset.
  Stored class-aware mean-CATE influence is the frozen-weight mixture, not the
  primary atom. Multi-threshold grids clear the scalar `ate` and that influence
  rather than leaving a first-threshold or mean-CATE number. `cell.aipw` joint
  responses publish per-arm `F_a(c)`. Exceedance grids cannot be reduced through
  the indicator helper to the first threshold. CustomDistribution +
  exceedance grid / quantile refuses instead of returning a mean of `Y`.
  First-click CustomDistribution of a multi-threshold grid stays refuse;
  licensed weighted grid is AllObserved prepare + retarget.
- Score artifacts retain covariance, simultaneous raw-score bands, tail event
  support, and target-local support. Bounded isotonic probabilities are disclosed
  on the estimate object (`monotone_rearranged`); the covariance and simultaneous
  bands describe the raw AIPW estimates and remain attached to their explicit
  `raw_means`. Only the scalar simultaneous interval is cleared on rearrangement;
  raw covariance is not uncertainty for the projected CDF. Multi-threshold grids do not publish a first-threshold scalar `ate`.
- Unknown tiers retain separate canonical-scenario effects and simultaneous
  intervals instead of a single averaged ATE. Two scenarios are not exhaustive
  completions or bounds on the unknown graph class. Unknown-tier prepare
  exports no score table (two canonical sets / GraphDependent / no single Z);
  one table would collapse the envelope, so `retarget` refuses. Converting
  Unknown tiers to an ADMG refuses rather than dropping within-tier
  uncertainty. CoDetermined estimates attach a VanderWeele + outcome-SD
  E-value approximation for the named no-latent-to-outcome premise
  (`tiered.evalue.vanderweele_approx`). This
  is not a tier-identification certificate; tier-closure is a fast path on a
  complete-tier ADMG. Bidirected CoDetermined used to default to
  `general.id` (AIPW refused). It now defaults to generalized adjustment +
  AIPW. Singleton-tier pins never hit this; same-tier siblings (`z↔u` or
  `t↔u`) are the real fixture.
- Static effect and batch covariance align influence rows in the original row
  universe. DAG `InterventionResponse` publishes a plugin IF and joint
  covariance; class-aware responses mix atom IFs only when every contributing
  identified atom supplies an IF aligned to original row IDs (including differing
  complete-case masks) and unidentified mass is zero. An unidentified completion
  NaNs the envelope SE rather than publishing a primary-atom interval.
  Additive GAM intervention IFs include coefficient-fitting and empirical
  covariate-average uncertainty conditional on fitted knots and the penalty. They
  exclude knot selection, smoothing bias, and policy integration error and are not
  nonparametric efficient IFs. Curve IF exports to joint covariance are scaled as
  row scores; local-polynomial diagnostic contributions retain their original scale.
- Batch results carry actual simultaneous intervals and BH/BY adjusted p-values.
  `PreparedBatch` / `BatchStudy::prepare` freeze per-query plans and one
  `SharedBatchDesign` (fold assignment; `[1 | Z…]` when certified adjustment
  sets agree). Propensity and outcome residualization stay per-query.
  `prepare_cells` covers discrete joint `InterventionResponse` on a DAG and on
  CoDetermined via linear candidate construction for fixed treatment count, followed by dense-ADMG verification: `Z` is the
  closure minus the treatments, verified with one m-separation check per
  treatment under `tiered.no_latent_to_outcome` (1.4-class ID, not MAG-as-MEC,
  not subset search). Python `PreparedBatch.prepare_cells` binds that path so a
  pair family shares folds and family joint IF. `{z}|{t1,t2}|{y}` stays the small fixture (`Z={z}`);
  a facet tier with ≥20 co-facets is identified the same way. Joint width
  evidence matches single-lever's class: a 200-node facet-clique pin (default;
  recorded bound 500 ms isolated / 1 s under the v15 suite; measured ~230 ms
  isolated) and a p=667 facet-clique
  ID bench (default; recorded bound 16 s, measured ~8 s unoptimized — the
  production bidirected clique, not the chunked single-lever 1 s bench). A budget cap on generic
  joint search is execution (`identify.generalized_adjustment.completion_capped`,
  counted in `truncated_completions`), not a new public identification status
  and not a scientific open-back-door. 1.5 does not add an
  `IdentificationStatus` variant or change its wire encoding. This does not
  establish forward compatibility for other new 1.5 payloads. A cap is execution, not a third identification outcome.
  Unknown-tier joint has no single ADMG.
  `analyze()` and `PreparedAnalysis.prepare` accept `TieredBackground` and
  keep outcome functionals (same honesty class as `PreparedBatch` /
  `analyze_many`). Candidate-selection provenance is recorded on
  `StudyResult`, on `EffectEstimate`, and on the estimate
  artifact wire (`screen_id`, procedure, winner, family size, screen/estimate
  row indexes, disjointness). When `estimate_rows` is recorded, estimation uses
  that analysis mask. The winner is ranked on the reported estimate-sample
  family; `screen_rows` are provenance. Candidate-selection provenance is still
  attached when joint IF cannot be formed. An unavailable ranking has `winner_index = None`,
  not an invented winner 0. Recorded splits require nonempty, unique, in-range rows;
  overlapping splits remain explicitly marked non-disjoint.
- Validator computation failures are typed `ValidationOutcome::Failed`, distinct
  from scientific rejection and incompatibility. Scalar Dag `InterventionResponse`
  licenses Frequentist cheap/full. `cell.aipw` cheap/full apply overlap and
  E-value / the effect-refuter suite to the requested-cell minus control-cell
  contrast. Plugin g-comp cheap is overlap only; full is overlap plus
  sampling-stability of the g-comp level (bootstrap, data-subset, graph).
  Contrast-shaped refuters (placebo, dummy outcome, RCC/UCC, E-value,
  sensitivity, Riesz) are not licensed for a plugin intervention level.
  `ResponseCurve` cheap/full stay n/a. Temporal and class-aware
  `InterventionResponse` cheap/full stay n/a.
- EconML column export actually transforms outcomes and exports aligned weights.
  Extra modifiers need pre-treatment certification, and nonconstant target weights
  require declared covariate parents.

## Consuming evidence

`crates/antecedent/tests/v15_numeric_pins.rs` covers requested joint cells,
retargeted cell agreement, new-data refresh, score/covariance/inference
artifact round trips, an 80-seed kernel-target coverage experiment, CPDAG
shared-row coverage, a two-MAG PAG response mix, a front-door MAG response identified
by Shpitser–Pearl ID, a median QTE pin, a PAG unidentified-completion
SE refusal, Dag and class-aware ConditionalEffect per-arm bands and tail
support, prepared batches (effects, joint cells, and a shared design object), estimate-row masking,
candidate-selection artifact provenance (including when joint IF cannot form),
Dag InterventionResponse cheap/full with estimator-specific refute semantics,
a named point-CDE refuse (`continuous_cell` is not `do(D=d0)`), CoDetermined
same-tier joint `{z}|{t1,t2}|{y}` licensed through closure candidate construction and dense-ADMG verification
+ cell-AIPW (`Z={z}`, `tiered.no_latent_to_outcome`; walking ↔ as a directed path is pinned to fail), a many-co-facet joint pin (≥20 same-tier facets; generic subset search would cap), a 200-node joint facet-clique ID pin and a p=667 joint ID bench (same evidence class as single-lever), a 200-node tier-closure + estimate-attached E-value pin,
named-interaction grid refusal, cleared grid scalars (no first-threshold `ate`),
CoDetermined `{z,u}|{t}|{y}` and `{z}|{t,u}|{y}` + ExceedanceGrid (batch
Mean/Exceedance equals standalone at 1e-12), AllObserved first-click vs
`retarget(ones)` agreement, ATT residualized refusal,
`depends_on` without a directed graph, plugin-full contrast-refuter strip, and the
linear-plan retarget refusal. Frozen fixtures
`conformance/estimate/v15_retarget_kernel`,
`conformance/estimate/v15_cell_aipw_interaction`, and
`conformance/estimate/v15_exceedance_variance` are parsed by those pins.
The kernel-target experiment uses the population functional
`1 + 0.6/(1 + 0.7²)`, not a sample-dependent proxy. An 80-seed CPDAG experiment
covers the frozen average of the two observational functionals (2 and 2.5); it
is not a posterior over completion-specific causal truths. These Monte Carlo
tests are finite evidence, not a universal coverage guarantee.

The ADR 0011 harness (`retarget_alloc_count.rs` and
`retarget_hot_path_10k_by_500_is_bounded`) measures 500 distinct kernel
levers on one prepared 10k-row table.

Estimate-crate tests cover constant-weight covariance equivalence, malformed
covariance rejection, weighted threshold interactions, and missing training
cells. Python `test_v15.py` and `test_econml_handoff.py` exercise public
wrappers, including `PreparedBatch.shared_design`, ConditionalEffect
`score_inference` bands / tail flags, and typed candidate-selection payloads
with screen/estimate row indexes.

## 1.5 contract status

| Requirement | Status |
|---|---|
| Frozen prepared score contract | Licensed for explicit iid AllObserved AIPW (`AverageEffect`) and cell-AIPW (`InterventionResponse`). First-click AllObserved iid AIPW uses the same cross-fitted φ table as `retarget(ones)`. ATT/trim/clustered AIPW stays full-sample residualized, exports no table, and refuses `retarget`. Linear, matching, IV, and Bayesian plans do not export scores and refuse `retarget`. Unknown-tier prepare exports no score table (two canonical sets / GraphDependent / no single Z); one table would collapse the envelope, so `retarget` refuses. First-click CustomDistribution of a multi-threshold grid or quantile refuses; weighted grids and QTEs use AllObserved prepare + retarget. Quantiles cover explicit AIPW AverageEffect, binary one-modifier ConditionalEffect on DAG/CPDAG/PAG, and explicit cell-AIPW joint response on DAG/CoDetermined. Conditional quantiles standardize arm CDFs over the modifier distribution; they do not export retargetable tables. All quantiles require Frequentist AllObserved and no refutation. Other estimator families refuse. `analyze()` does not always return scores. Plugin g-comp IFs on DAG responses are not AIPW φ and are not retargetable. Nonempty `depends_on` without a directed-graph (DAG or ADMG) descendant closure is refused. Nonconstant target weights require a nonempty `depends_on`. |
| Full functional surface | ConditionalEffect grids publish bounded monotone per-arm `F_a(c)`, 2K raw-score joint covariance, max-t bands, and per-threshold tail support. Unsupported tails are non-finite rather than an empty-cell or first-threshold SE. Rearrangement is disclosed; bands stay on raw `F_a`. Multi-threshold `ate` stays NaN. Quantile treatment effects invert a piecewise-linear AIPW CDF (`OutcomeFunctional::Quantile`), conditional on the finite estimation grid. Inference excludes interpolation bias and grid-selection uncertainty; crossings altered by isotonic projection or lacking tail/overlap support refuse. Binary ConditionalEffect arm IFs are AIPW. |
| Joint estimator | Point CDE is a named refuse (`estimate` / `continuous_cell` is not `do(D=d0)`). Scalar Dag InterventionResponse cheap/full are licensed support-matrix cells with estimator-specific refute semantics (cell.aipw contrast vs plugin overlap / overlap+stability). |
| Tier closure | 200-node certified-set pin and p=667 ID bench exist for single-lever (chunked, 200 ms / 1 s) and for joint `do(t1,t2)` on a CoDetermined facet clique (unoptimized test ceilings 500 ms / 16 s; the final optimized p=667 run took 0.46 s). The attached CoDetermined number is the usual VanderWeele approximation, not a new ID result. Bidirected CoDetermined defaults to generalized adjustment + AIPW, not `general.id`. |
| Static covariance | Response scaling/alignment regressions and plugin coefficient-uncertainty coverage exist. PAG `InterventionResponse` mixes aligned IFs on a two-MAG class (`R→T`, `Z o→ T`, `Z→Y`, `T→Y`); an unidentified completion in the shielded `Z o-o T` class NaNs the envelope SE rather than publishing a primary-atom interval. The older fully-directed PAG pin remains a single-atom IF attachment test, not the multi-atom evidence. A front-door MAG (`T→M→Y`, `T↔Y`) is identified by Shpitser–Pearl ID after adjustment fails. |
| Prepared batch | Frozen plans execute in bounded parallel chunks and preserve order. `SharedBatchDesign` is one fold-assignment object; A base `[1 | Z…]` matrix is shared when certified adjustment sets agree and rebuilt once per new batch table. Per-query preparation still materializes an initial design before gathering/replacing it with the shared values; this is not an allocation-free preparation path. Propensity and outcome residualization are still fit per query. Pins fail if sharing was requested and each query silently rebuilt an independent design. `analyze()` and `PreparedAnalysis.prepare` accept `TieredBackground` and keep outcome functionals (same honesty class as `PreparedBatch` / `analyze_many`). `prepare_cells` is licensed on a DAG and on CoDetermined: joint `do(A,B)` constructs the treatment-set closure (minus treatments) in O(p) for fixed joint width, then builds the dense ADMG and verifies it with one m-separation check per treatment under `tiered.no_latent_to_outcome` (not MAG-as-MEC, not subset search). `{z}|{t1,t2}|{y}` is the small fixture (`Z={z}`, cell-AIPW + interaction); a facet tier with ≥20 co-facets uses the same shortcut. A pair that fails the closure check (open back-door or drawn T↔Y) is a scientific refuse; a generic-search budget cap is execution (`identify.generalized_adjustment.completion_capped`), not a new public status and not that refuse. Compatibility: no new `IdentificationStatus` variant or change to its wire encoding; this is not blanket forward compatibility for new 1.5 payloads. A cap is execution, not a third identification outcome. Cross-tier pairs use the same criterion. Unknown-tier joint has no single ADMG. Python `PreparedBatch.prepare_cells` binds the DAG / CoDetermined path; two or more pairs share folds and attach family joint IF (max-t / BH / BY). Bare ADMG InterventionResponse already refuses (`no functional plug-in`). PAG-native ID/IDC remains never. |
| Claim hygiene | Candidate selection is on `StudyResult`, `EffectEstimate`, the estimate artifact wire, and Python `EstimateSection`, including screen/estimate row indexes. |
| Release contract | Dedicated `v15_*` fixtures are consumed by executing pins. Support-matrix and conformance docs were regenerated. Generated output is idempotent; the release script's clean-diff check remains pending while these reviewed release-note edits are uncommitted. |

## Final-review regressions

The zero-shift plugin identity now checks both the sample mean and its sampling
SE. A 40-seed noisy constant-outcome-model experiment checks Set-policy interval
coverage (at least 33/40); it previously produced zero SE. A curve regression
compares exported local-WLS contributions with joint covariance, including the
finite-sample correction. A row-alignment test mixes disjoint original row sets.
The 200-node SCM now obeys CoDetermined within-tier assumptions, independently
checks m-separation in the intervention backdoor graph, and checks the known
1.8 effect against its interval. These are finite tests, not universal coverage.

Conditional CDF values are projected into [0, 1] and made monotone per arm.
Joint covariance and simultaneous bands are the 2K raw per-arm CDF
coordinates. Rearranged `exceedance_cdf` values are not mixed with those
intervals. Binary 0/1 ConditionalEffect arm IFs are cross-fitted AIPW
(nonparametric EIF under back-door, positivity, and nuisance rates).
Non-binary treatment levels keep the linear plugin IF.

## Explicitly deferred

Temporal/DBN multi-atom Frequentist uncertainty remains unavailable until its
separate 1.9 owner work. The 1.4 limitation text is kept verbatim for those
cells. This is not a 1.5 open caveat. PAG-native ID/IDC remains never.
MAG/PAG *response* identification in 1.5 is complete-then-ID: generalized
adjustment first, then Shpitser–Pearl ID on each MAG-as-ADMG. The front-door
MAG pin (`T→M→Y`, `T↔Y`) is identified by ID after adjustment fails and
recovers the intervention means 0.3 at Set(0) and 0.6 at Set(1). The two-MAG
all-identified fixture still uses the same `{Z}` adjustment on both
completions — that pin is mixed-IF honesty, not a different functional.
Quantile treatment effects invert the AIPW CDF on a disclosed grid; finite-grid approximation and frozen-grid uncertainty are distinguished from exact population quantile inference.

## Mathematical scope

AIPW influence inference requires positivity, suitable nuisance limits and rates,
and the stated sampling assumptions. Cross-fitting controls empirical dependence
but does not correct arbitrary joint misspecification. See
[Chernozhukov et al., Double/debiased machine learning](https://arxiv.org/abs/1608.00060).
Graph certification must be valid for the stated graph class and conditioning
set; two selected scenarios do not prove a class-wide bound. See
[Perković et al., generalized adjustment](https://jmlr.org/papers/v18/16-319.html).

The fitted-basis response sandwich separates coefficient uncertainty from
smoothing choices; see [mgcv covariance documentation](https://www.stat.ethz.ch/R-manual/R-devel/library/mgcv/html/vcov.gam.html).

## Final-review validation (2026-09-10)

- `cargo test --workspace --no-fail-fast -- --test-threads=1`: 1,835 passed,
  zero failed, 31 ignored (including documentation tests).
- `cargo clippy --workspace --all-targets -- -D warnings` and formatting pass.
- Python against the rebuilt release extension: 1,295 passed. Mypy passes for
  37 source files; Ruff passes.
- Conformance/support-matrix regeneration changes no generated output. All prior
  feature gates pass. The combined release script stops at its clean-diff check
  because the reviewed release notes are intentionally uncommitted; this is not
  a complete release-gate pass.
- All 12 subsequent release benchmark smokes pass when run separately.
- Offline cargo-deny audit passes using the existing advisory cache copied to
  a writable temporary directory; this is not a freshly fetched advisory audit.

Focused 1.5 gates after the three completion items: `v15_numeric_pins`
(DAG cells plus the CoDetermined closure-ADMG cell-AIPW pin and point-CDE refuse), `antecedent-estimate --lib`, related class-aware/response pins,
`cargo clippy` on the touched crates, and Python `test_v15.py` (17) plus
`test_frozen_namespace.py` against a rebuilt release extension. This is
not a fresh full-workspace release-gate pass.

## Follow-up review (2026-09-11)

- Conditional exceedance outcomes are transformed exactly once. Translating Y
  and every threshold together preserves raw CDFs on DAG and CPDAG paths.
- Prepared batches rebuild one shared design on each new data table; individual
  plan estimates and refreshes also replace data-dependent covariates and folds.
- Prepared QTEs retain threshold scores, and estimate/refresh/retarget perform
  quantile inversion. Constant weights agree with first-click QTE; multiplying
  target weights by a constant preserves both the QTE and its SE.
- Quantile inversion validates all coordinates and refuses projection-altered
  crossings rather than assigning raw-score uncertainty to a projected inverse.
  The threshold grid is derived from complete estimation rows, not excluded rows.
- Conditional class tail support must hold in every contributing atom. Scalar
  empty-tail uncertainty is unavailable, and unavailable bands survive artifact
  decoding. Binary conditional score fits enforce propensity-overlap support.
- Invalid shared fold IDs refuse instead of leaving some rows with fabricated
  zero scores. Response ID fallback errors propagate as errors, not scientific
  non-identification.
- A new 40-seed two-MAG response experiment measures interval coverage of the
  known population functional 1.6 and checks that identical atom scores retain
  their full shared-row covariance. Both MAGs use Z adjustment, so this does not
  demonstrate mixing different identified functionals or general PAG coverage.

The earlier validation counts above are historical; follow-up validation is
recorded separately below.

The final general-ID response review corrected a binary ATE being returned for
a requested intervention mean. The front-door regression now checks both
requested Set levels separately (0.3 and 0.6), rather than accepting their 0.3
contrast as a level. General-ID response estimation explicitly refuses
unsupported target populations and non-scalar outcome functionals.

Follow-up validation:

- Full Rust workspace: 1,880 passed, zero failed, 32 ignored. After the final
  general-ID mean correction, identification tests and all 60 active 1.5
  numerical pins pass again (one opt-in pin ignored).
- Rebuilt Python release extension: 1,302 tests passed. Mypy passes for 37
  source files; Ruff lint and formatting pass for all 149 checked files.
- Strict workspace/all-target Clippy and Rust formatting pass.
- Support-matrix and release-note generation is idempotent, checked against
  hashes of the edited files before regeneration.
- CodeQL security-and-quality scans pass for Rust, Python, and Actions with
  zero findings after the repository's existing query exclusions; no exclusions
  were added. The final Rust source was rescanned after the last fix: 585 files
  extracted without errors.
- All prior release feature gates pass with `UV_NO_SYNC=1` against the rebuilt
  local extension. The combined script stops at its clean-diff check because
  the reviewed release notes are intentionally uncommitted; regeneration is
  idempotent, but this is not a complete release-script pass.
- Offline cargo-deny passes using the existing advisory cache in a writable
  temporary directory; this is not a freshly fetched advisory audit.
- All 12 remaining release benchmark smokes and the release artifact tests pass
  when run separately after the clean-diff stop.

Complexity scope: joint tier certification constructs its candidate closure in
O(p) for a fixed number of treatments, then materializes the complete-tier ADMG and checks generalized adjustment.
The whole joint-identification operation is therefore not established as O(p):
the dense graph can contain O(p²) edges. The 667-variable benchmark is a measured
runtime pin, not proof of linear asymptotic complexity. Single-treatment closure
identification avoids that joint graph-construction path.

The optimized 667-variable joint identification test passes in 0.46 seconds
(`cargo test -p antecedent-identify --release --lib
joint_identification_p667_under_recorded_bound`). This meets the one-second
runtime target on this machine; the existing unoptimized test allows 16 seconds.
The final rebuilt Python extension again passes all 1,302 tests.

## Documentation verification for release preparation

The runnable Python walkthrough distinguishes AIPW score-table inference
(exceedance coordinates) from conditional-grid inference (CDF coordinates).
`exceedance_cdf` contains projected CDF values in both cases. For the former,
complement the raw mean and reverse/complement the bounds to obtain CDF bands.
Always honor the support flags: AIPW score-table results can retain finite bounds
for unsupported coordinates, whereas the reviewed conditional-grid path marks
such bounds NaN. The earlier conditional-grid NaN evidence is not a promise that
every score-table path erases its unsupported numerical bounds.

The three walkthrough blocks execute successfully against the rebuilt local
1.5 extension (retarget, weighted CDF/support, and CoDetermined joint response).
That documentation pass exposed a direct DAG `cell.aipw` dispatch refusal.
The subsequent quantile implementation pass fixes both the public dispatcher and
prepared native bridge, and forwards outcome functionals on DAG and CoDetermined
joint response paths. The walkthrough now uses the direct DAG entry point.

## Conditional and joint quantile completion

The subsequent implementation pass extends finite-grid inversion to binary
one-modifier ConditionalEffect on DAG/CPDAG/PAG and discrete joint cell-AIPW
InterventionResponse on DAG/CoDetermined. Conditional queries invert the arm CDFs
standardized over the retained modifier distribution. Class mixtures combine raw
CDF coordinates and aligned row influences before inversion; unidentified mass
refuses. Joint queries invert the requested cell, preserving zero-valued Set
coordinates, and return a quantile level rather than a treatment contrast.

Consuming regressions cover:

- Conditional median effects on a location-shift SCM and a skewed SCM with equal
  medians but different means, plus DAG/CPDAG agreement and a two-atom PAG.
- Joint mean and median levels with a non-additive interaction and skewed noise,
  on both Python DAG and CoDetermined entry points.
- Fresh/prepared/retarget agreement, invariance to scaling target weights, outcome
  translation, and refresh replacing the retained score table.
- The quantile influence derivative against central differences of a perturbed
  CDF on a fixed grid. This checks the delta-method calculation, not population
  coverage under arbitrary nuisance misspecification.
- Explicit refusals for mean-effect refuters and mean-only response estimators;
  the Python bridge must not discard a requested quantile functional.

All quantile paths require Frequentist inference and `refute=none`; E-values
for mean effects are not attached. The initial population is AllObserved.
Average-effect and joint score tables support prepared retargeting; conditional
plans do not export retargetable scores. These additions do not license Bayesian,
IV, matching, or plugin mean estimators for quantile inference, pointwise modifier
quantile surfaces, or general-ID quantiles. Finite-grid interpolation bias and
grid-selection uncertainty remain excluded.

The rebuilt CPython 3.13 extension passes all **1,315 Python tests**, with Ruff
and mypy clean. All five Python walkthrough blocks execute successfully. The
strict documentation build also passes. Earlier validation counts above describe
earlier review snapshots, not additional runs of this expanded implementation.

The full Rust workspace run passed **1,884 tests**, with **32 ignored**. After
adding explicit conditional-grid diagnostics, all six consuming quantile pins
passed again; all five quantile unit tests, including the numerical derivative
check, passed. The conditional diagnostic publishes the actual grid and arm order
without pretending that a conditional plan exports a retargetable score table.

Strict all-target Clippy passes. After the final extension rebuild, all **32**
Python 1.5 feature tests and all five walkthrough examples pass again. The release
gate passes its inventory and prior feature checks, then stops at the uncommitted
release-note clean-diff check. Regenerating the support outputs is idempotent;
this is not a claim that the combined release gate passed. The twelve benchmark
smokes reported earlier were not repeated in this quantile pass.

The repeated CodeQL Rust/Python/Actions scan reports **zero findings** under the
unchanged repository exclusions. Its Rust extraction preceded the final addition
of conditional threshold-list diagnostics; that diagnostic-only Rust change is
covered by Clippy and the consuming tests above. CodeQL must still run on the
committed release candidate. No new query exclusions were introduced.
