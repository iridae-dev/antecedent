# pinned baseline noisy multi-estimator conformance

**Suite path:** `conformance/estimate/noisy_estimators`

# Generated by scripts/conformance/generate_baseline_noisy_estimators.py

Noisy SCMs with pinned baseline (or clean-room AIPW) `val`/`se` pins.

## Expected summary

Top-level keys: `atol_se, atol_val, clip, estimator_map, generation, methods, n, reference, rtol_se, rtol_val, scenarios, seed, tolerance_class, true_ate` (14 fields).
