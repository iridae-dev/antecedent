# RPCMCI two-regime pin

**Suite path:** `conformance/tigramite/rpcmci_two_regime`

Typed regime assignments yield exactly one graph per regime (no single-graph collapse).

## Expected summary

Top-level keys: `algorithm_id, min_links_per_regime, min_nodes_per_regime, n_regimes, notes, tolerance_class` (6 fields).
