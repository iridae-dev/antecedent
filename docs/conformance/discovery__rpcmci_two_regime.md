# RPCMCI two-regime pin

**Suite path:** `conformance/discovery/rpcmci_two_regime`

Typed regime assignments yield exactly one graph per regime (no single-graph collapse).

## Expected summary

Top-level keys: `algorithm_id, generation, min_links_per_regime, min_nodes_per_regime, n_regimes, notes, reference, tolerance_class` (8 fields).
