# DoWhy noisy multi-estimator conformance

**Suite path:** `conformance/dowhy/noisy_estimators`

# Generated by scripts/conformance/generate_dowhy_noisy_estimators.py

Noisy SCMs with DoWhy (or clean-room AIPW) `val`/`se` pins.

## Expected summary

Top-level keys: `atol_se, atol_val, clip, dowhy, estimator_map, generation, methods, n, rtol_se, rtol_val, scenarios, seed, tolerance_class, true_ate` (14 fields).
