# Vector-variable PCMCI conformance fixture

**Suite path:** `conformance/tigramite/vector_vars_pcmci`

Synthetic Exact edge-set pin for `tigramite.data.vector_variables`.
`x0`,`x1` form one vector group (logical node `x0`); recover `x0@1 → y`.

Comparison class: Exact (recovered lagged edge set over logical nodes).

## Expected summary

Top-level keys: `alpha, fdr, generation, max_lag, n, scm, tolerance_class, true_parents, vector_groups` (9 fields).
