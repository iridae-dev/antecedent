# PCMCI multi-var conformance

**Suite path:** `conformance/discovery/pcmci_multivar`

≥4 variables; compares edge sets and val/p/(q) matrix entries.
Oracle regeneration: see `expected.json` → `reference.command` (out-of-repo).

## Expected summary

Top-level keys: `alpha, atol_p, atol_stat, fdr, generation, max_lag, n, reference, rtol_p, rtol_stat, scm, tolerance_class, true_parents, variables` (14 fields).
