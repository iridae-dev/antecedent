# gcm_anomaly

**Suite path:** `conformance/gcm/gcm_anomaly`

Fitted SCM with one planted outlier on Y; anomaly score at that unit exceeds
the score at a typical unit.

## Expected summary

Top-level keys: `generation, planted_outlier_index, reference, require_outlier_gt_baseline, tolerance_class` (5 fields).
