# J-PCMCI+ two-environment pin

**Suite path:** `conformance/tigramite/jpcmci_plus_two_env`

Pooled Günther-style multi-environment discovery must report `algorithm_id = jpcmci_plus`
with at least two environments and a context-aware temporal CPDAG (≥2 nodes).

## Expected summary

Top-level keys: `algorithm_id, min_envs, min_links, min_nodes, notes, require_true_edge_subset, tolerance_class, true_links` (8 fields).
