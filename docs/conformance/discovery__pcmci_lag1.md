# PCMCI lag-1 conformance fixture

**Suite path:** `conformance/discovery/pcmci_lag1`

Baseline pin: `parity/baselines/tigramite.toml`.
Comparison class: Exact (recovered lagged edge set).
Oracle regeneration: see `expected.json` → `reference.command` (out-of-repo).

## Expected summary

Top-level keys: `alpha, fdr, generation, max_lag, n, reference, scm, tolerance_class, true_parents` (9 fields).
