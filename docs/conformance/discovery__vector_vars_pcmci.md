# Vector-variable PCMCI conformance

**Suite path:** `conformance/discovery/vector_vars_pcmci`

Logical vector groups with Exact parent-set pin.
Oracle: see `expected.json` → `reference`.

## Expected summary

Top-level keys: `alpha, fdr, generation, max_lag, n, reference, scm, tolerance_class, true_parents, vector_groups` (10 fields).
