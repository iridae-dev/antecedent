# Functional validation

Uses the frozen discrete chain and confounded binary count table in the staged prepared-analysis tests. The path effect is 1; the intervention mean is 0.7. Validation compares the actual path functional or entire conditional probability tables. Subset checks are descriptive robustness checks, not independent proof of identification.
