# pinned baseline linear-Gaussian ATE conformance fixture
#
# Frozen linear-Gaussian ATE fixture (temporary generator deleted after artifact import)
# Baseline pin: parity/pinned baseline.toml (pinned baseline 0.14).
# Comparison class: StableFloat (ADR 0010).
