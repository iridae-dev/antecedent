# J-PCMCI+ two-env edge-set pin (tigramite black-box)

System-only Günther J-PCMCI+ vs pinned `tigramite.jpcmciplus.JPCMCIplus`
(`include_space_dummy=false`). Regenerated by:

```bash
uv run --python 3.10 --with numpy --with joblib --with 'tigramite==5.2.9.7' \
  python scripts/conformance/generate_tigramite_jpcmci_plus_two_env_edges.py
```

Requires a tigramite build that ships `jpcmciplus` (PyPI ≥5.2.9; 5.2.1.30 does not).
Space-dummy multivariate CI parity is P4.4b, not this fixture.
