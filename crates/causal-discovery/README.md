# causal-discovery

Static and temporal causal discovery. Phase 2 complete: PCMCI (PC parents +
MCI), compiled constraint masks (`required` / `forbidden` / `tiers`), FDR over
the full MCI family, target-wise parallelism, and `TemporalGraphReview` output.
