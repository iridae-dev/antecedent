# causal

Unified `CausalAnalysis` facade: static ATE and temporal effect identify → estimate
(→ refute) with logical/physical planning (DESIGN.md §3 / §21). Python package name
is also `causal` (PyO3 crate `causal-py`).
