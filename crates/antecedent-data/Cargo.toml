[package]
name = "antecedent-data"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Causal data views and Arrow-backed adapters for the Antecedent causal inference engine; start with the `antecedent` crate"
readme = "README.md"

[lints]
workspace = true

[features]
default = ["arrow"]
arrow = [
    "dep:arrow-array",
    "dep:arrow-schema",
    "dep:arrow-buffer",
    "dep:arrow-data",
    "arrow-array/ffi",
    "arrow-schema/ffi",
]

[dependencies]
antecedent-core = { version = "0.5.2", path = "../antecedent-core" }
antecedent-kernels = { version = "0.5.2", path = "../antecedent-kernels" }
arrow-array = { workspace = true, optional = true }
arrow-schema = { workspace = true, optional = true }
arrow-buffer = { workspace = true, optional = true }
arrow-data = { workspace = true, optional = true }
thiserror = { workspace = true }
realfft = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
