[package]
name = "causal-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Probabilistic and structural causal models, mechanisms, and sampling"
readme = "README.md"

[lints]
workspace = true

[dependencies]
causal-core = { version = "0.1.0", path = "../causal-core" }
causal-data = { version = "0.1.0", path = "../causal-data" }
causal-graph = { version = "0.1.0", path = "../causal-graph" }
causal-kernels = { version = "0.1.0", path = "../causal-kernels" }
causal-prob = { version = "0.1.0", path = "../causal-prob" }
causal-stats = { version = "0.1.0", path = "../causal-stats" }
thiserror = { workspace = true }

[features]
default = []
# Gaussian-process mechanism family.
gaussian-process = []

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "sample_overlay"
harness = false
