[package]
name = "causal-prob"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Probability distributions, columnar posteriors, priors, and inference backends"
readme = "README.md"

[lints]
workspace = true

[dependencies]
causal-core = { path = "../causal-core" }
causal-kernels = { path = "../causal-kernels" }
thiserror = { workspace = true }

[features]
default = ["hmc"]
# Native HMC sampling backend (DESIGN.md §30). Empty feature flag; always compiled when enabled.
hmc = []
# Reserved for SMC (DESIGN.md §30); not yet implemented as a separate backend.
smc = []

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "laplace_glm"
harness = false
